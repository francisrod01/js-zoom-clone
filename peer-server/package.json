{
  "name": "peer-server",
  "version": "0.0.1",
  "description": "A peer-server for real-time custom events",
  "main": "index.js",
  "scripts": {
    "dev": "PORT=9000 npm start",
    "start": "npx peerjs --port $PORT --key peerjs --path /",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Francis Rodrigues",
  "license": "MIT",
  "dependencies": {
    "peer": "0.6.1"
  }
}
